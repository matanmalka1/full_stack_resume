/* The drafts feature's public surface: the routed screen, and nothing else. Validation,
   approval, and render are states of that screen rather than components other features
   compose, and the selection vocabulary the fact panel says lives with the SelectionPlan
   in `@/features/applications/model/selectionLabels`. */
export { DraftEditorPage } from "./pages/DraftEditorPage";
