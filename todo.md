# TODO — שיפורי UX ב־Frontend

הרשימה מרכזת 23 משימות: עשר המשימות המקוריות ו־13 ממצאים נוספים, עם גבולות עבודה ותנאי סיום לכל אחת. זו תוכנית עבודה; משימה פתוחה אינה אומרת שכל הפערים שבה כבר אומתו.

**עדכון מול הקוד: 14.09.2026.** ביטול וניסיון חוזר, רכיב דיאלוג משותף ורענון חלק מהנתונים אחרי אישור עובדה כבר קיימים. בסעיפים האלה יש לבדוק ולהשלים את הפער שנותר.

## כללי עבודה וסגירה

- **P1 — עדיפות גבוהה:** תהליך הכנת קורות החיים ושימוש יומיומי במסמך.
- **P2 — שיפור:** נוחות, חיפוש ואחידות ממשק.
- **P0 — חסם תהליך:** דרך מפורשת לפתרון החלטות שחוסמות אישור; יש לטפל לפני שיפורי נוחות.
- לפני מימוש מאמתים את הפער בקוד ובתרחיש שימוש. אם הוא כבר מכוסה, מתעדים את הכיסוי וסוגרים בלי לממש מחדש.
- הרשימה אינה אישור לשינוי חוזי המוצר. שינוי בזרימה, בהרשאות או במשמעות מצב חייב להתאים למפרט; סתירה לא פתורה היא חסם.
- React פונה ל־FastAPI; לוגיקת שימוש והרשאות נשארת בשכבת היישום. ה־API אינו מתחיל עבודת רקע בעצמו.
- אין לשנות או לדרוס עובדות, JobSnapshot, גרסה מאושרת, ארטיפקט או רשומת הגשה קיימים במסגרת שיפורי UX.


## מפת משימות

| מזהה | עדיפות | משימה | מצב התחלתי | בירור / תלות מרכזיים |
| --- | --- | --- | --- | --- |
| UX-01 | P1 | זום וגלילה במסמך | פער מאומת בבקרות התצוגה | שמירת פלט הרינדור הקיים |
| UX-02 | P1 | Undo / Redo בטיוטה | לתכנון מול מנגנון השמירה | היסטוריה מקומית, autosave וקונפליקטים |
| UX-03 | P1 | עריכת הגדרות התאמה | לבדוק נגישות והרשאות | החלטות ניתוח לעומת ניתוח חדש |
| UX-04 | P1 | ביטול וניסיון חוזר | ממומש חלקית — לבדוק פערים | פעולות מותרות והתקדמות מהשרת |
| UX-05 | P1 | עדכון אחרי אישור עובדה | הושלם; הבדיקות והסטטיקה עברו | שינוי תוכנית, staleness ואימות מדויק |
| UX-06 | P1 | שחזור הקשר מרענון וקישור ישיר | הושלם; הבדיקות הממוקדות וה־lint עברו | URL, הקשר פעיל ו־Operations מהשרת |
| UX-07 | P2 | העתקה כטקסט / Markdown | פתוח | מקור מפורש: טיוטה או גרסה מאושרת |
| UX-08 | P2 | הרחבת החיפוש הגלובלי | מועמדויות מוגבלות ל־8 תוצאות | תמיכת API בעובדות ובהיסטוריה |
| UX-09 | P2 | ולידציה בעדכון משרה | לבדוק ולהשלים | טקסט מדויק ו־Snapshot חדש |
| UX-10 | P2 | אחידות דיאלוגים ופוקוס | רכיב משותף כבר קיים | חריגים ומדיניות סגירה |
| UX-11 | P0 | פתרון החלטות סקירה מתוך הטיוטה | הושלם; הבדיקות והסטטיקה עברו | פתרון לפי הסיבה, שמירה, רענון ו־staleness |
| UX-12 | P1 | שינוי סדר סעיפים ושורות | אין פקדי הזזה ברכיבים שנבדקו | חוזה שמירה, מגבלות מבנה ורינדור |
| UX-13 | P1 | היסטוריית נוסחי משרה והשוואה | הפאנל מציג את התצלום האחרון | גישה לתצלומים מדויקים ללא שינוי מקור |
| UX-14 | P1 | אזור מרוכז לפרטי קשר | לבדוק הצגת עובדות הזהות והקשר | תיקון עובדות בלבד; אין candidate CRUD |
| UX-15 | P1 | ניהול מרוכז ודפדוף במאגר עובדות | הרשימה מרונדרת במלואה, בלי בחירה קבוצתית | פעולות מפורשות וכשל חלקי |
| UX-16 | P1 | טעינה, שגיאה ו־Retry בכניסה למועמדות | הושלם; הבדיקות הממוקדות וה־lint עברו | Retry מפורש אחרי כשל סופי; 404 נשמר |
| UX-17 | P2 | היסטוריית גרסאות מאושרות והשוואה | המסך פונה לגרסה אחת לפי URL | קישור לרשומות ולארטיפקט שנמסר בפועל |
| UX-18 | P2 | כיווניות תוכן בטיוטה | dir="auto" כבר קיים בשורות | שפת המסמך, יישור וטקסט מעורב |
| UX-19 | P1 | הקשר נכון בייצוא תיעוד החלטות | הזרימה האמיתית מעבירה מזהה; ממצא mock לא אומת | שיוך גרסה ורשומת החלטות היסטורית |
| UX-20 | P1 | התאוששות מקונפליקט בהגדרות | אין טיפול ייעודי ב־412 בטופס | שימור עריכות והשוואה מול ETag חדש |
| UX-21 | P2 | הסרת תלות ב־sessionStorage להמשכי עבודה | שימוש ישיר קיים לצד מצב שרת ו־idempotency | בידוד מועמדויות, רענון וחסימת Storage |
| UX-22 | P2 | איחוד העדפת ערכת נושא עם Settings | Theme מקומי; צפיפות וגודל טקסט בשרת | מקור אמת, העברת העדפה וחוזה Settings |
| UX-23 | P2 | חיווי ניתוק רשת וחוסר זמינות שרת | אין חיווי גלובלי ב־AppLayout | רשת לעומת שרת, שמירת עריכות והתאוששות |

## P0 — חסם תהליך

### UX-11 — פתרון החלטות סקירה מתוך עורך הטיוטה

- [x] **השלמת המשימה**

**מטרה:** משתמש שהאישור שלו חסום בגלל החלטה פתוחה יקבל פעולה ברורה לפתרונה, בלי לנחש היכן נמצא טופס ההחלטות.

**בוצע:** פתרון החלטות inline לפי הסיבה, גלילה ופוקוס לשורות עובדה, שמירת עריכות לפני פעולה ורענון ההקשר לאחר פתרון. חסמי שרת ו־staleness ממשיכים למנוע אישור.


**מפרט מחייב:** `docs/spec/state-and-use-cases.md`, סעיפים 7–9, 13 וחוזה אישור הטיוטה.

**אזורי עבודה:** `DraftEditorPage`, `DraftApprovalBar`, `ReviewDecisionPanel`, `ReviewDecisionForm`, ניתוב המועמדות ופעולות פתרון עובדות.

**צעדים לביצוע:**

- להציג כרטיס החלטות פתוחות עם הסיבה, השלכתה על האישור ופעולת הפתרון שהשרת מאפשר.
- עבור החלטות ניתוח כגון `HARD_GAP_REQUIRES_DECISION` או דו־משמעות מהותית, להשתמש בטופס ההחלטות הקיים inline, או בקישור ישיר שמביא אליו עם גלילה ופוקוס. לבחור פתרון אחד ברור לשלב הראשון.
- לא להפנות כל סיבה לאותו טופס: סיבות הקשורות לעובדות או להשלמת ידע צריכות פעולה מתאימה לסוגן.
- ליישב עריכות מקומיות לפני ניווט או החלפת הקשר, ולטפל בכשל שמירה ובקונפליקט בלי לאבד טקסט.
- לרענן את המועמדות והטיוטה אחרי פתרון. אם נוצר Analysis או SelectionPlan חדש, להציג staleness והמשך נדרש במקום לפתוח אישור אוטומטית.

**תנאי סיום:** לכל סיבה מוצגת דרך פתרון מותרת, או הסבר ממוקד כשאין פעולה זמינה. אפשר להגיע להחלטה ולחזור לטיוטה בלי אובדן עריכות. האישור נפתח רק כשכל החוסמים הוסרו ויש אימות תקף לגרסה המדויקת; אין עקיפת חוסמי השרת.

**אימות למסירה:** פער חריג, דו־משמעות מהותית, עובדה ממתינה, כמה סיבות במקביל, עריכות לא שמורות ושינוי הקשר בעקבות החלטה. להתחיל מהכיסוי ב־`DraftEditorPage.test.tsx` ובבדיקות ההחלטות. קשור ל־UX-03 ול־UX-05, אך אינו מחליף אותם.

## P1 — עדיפות גבוהה

### UX-01 — זום וגלילה בתצוגת המסמך

- [ ] **השלמת המשימה**

**מטרה:** לאפשר קריאה נוחה של קורות החיים בעמודה צרה ובמסך נייד.

**מצב קיים:** `DocumentFrame` שומר על מידות A4, מכווץ את התצוגה לרוחב הזמין ומשתמש ב־`overflow-hidden`. אין בו בקרות זום אינטראקטיביות.

**מימוש ממתין לאימות:** נוספו התאמה דינמית לרוחב, זום ידני 25%–200% בקפיצות של 10%, חזרה ל־100% ואזור גלילה ממוקד־מקלדת. ה־iframe נשאר במידות A4 המקוריות וההתאמה היא תצוגתית בלבד. בדיקות הרכיב נכתבו; שערי המשתמש טרם הורצו.

**אזורי עבודה:** `frontend/src/ui/DocumentFrame.tsx`, `DraftWorkspace`, `RevisionPage` ושימושים נוספים ברכיב.

**צעדים לביצוע:**

- להוסיף מעל התצוגה בקרות `100%`, התאמה לרוחב, הגדלה והקטנה, והצגת הזום הנוכחי.
- להגדיר טווח זום וקפיצות קבועות; התאמה לרוחב תתעדכן בשינוי גודל החלון.
- לאפשר גלילה אופקית ואנכית כשהמסמך גדול מהשטח הזמין, גם במגע ובמקלדת.
- לשמור על מידות המסמך המקוריות ועל כיוון תוכנו, בנפרד ממעטפת הממשק בעברית.

**תנאי סיום:** אפשר לקרוא ולהגיע לכל אזורי המסמך במסך צר וב־100%. הבקרות נגישות במקלדת ובעלות שם ברור. זום אינו משנה את פריסת המסמך או את HTML/PDF המופקים.

**אימות למסירה:** זום, שינוי רוחב, גלילה ו־RTL בתצוגות טיוטה וגרסה, עם בדיקות פרונט ודפדפן ממוקדות. שינוי בקרות התצוגה בלבד אינו מחייב golden או בדיקות בקאנד; אם משתנה רינדור הארטיפקט עצמו, חל שער הרינדור ב־`AGENTS.md`.

### UX-02 — Undo / Redo בעריכת טיוטה

- [ ] **השלמת המשימה**

**מטרה:** לשחזר ניסוח קודם ולהחזיר עריכה שבוטלה, בלי לאבד שינויים או לפגוע בשמירה.

**מצב קיים:** `useDraftEditing` נשען על `useDraftAutosave`, הכולל הוספות, הסרות וקונפליקטים. ה־debounce המוגדר כיום הוא **700ms**, ולא 1.5 שניות כפי שנכתב ברשימה המקורית.

**אזורי עבודה:** `useDraftEditing`, `useDraftAutosave`, רכיבי עריכת השורות ו־`DraftWorkspace`.

**צעדים לביצוע:**

- להגדיר יחידת Undo: רצף הקלדה, החלפת ניסוח, הוספה או הסרה. לציין אילו פעולות נכללות בשלב הראשון.
- לנהל היסטוריה מקומית מוגבלת ולנקות את ענף ה־Redo אחרי עריכה חדשה.
- להוסיף כפתורי Undo / Redo ושחזור שורה, וקיצורים מתאימים ל־Windows ול־macOS.
- לשלב את השחזור בתור השמירה הקיים, לפני שמירה וגם אחריה; שחזור תוכן שכבר נשמר הוא עדכון חדש של הטיוטה.
- להגדיר התנהגות בזמן שמירה, מעבר לטיוטה אחרת וקונפליקט ETag. לשמר Undo טבעי בשדות שאינם חלק מהיסטוריית הטיוטה.

**תנאי סיום:** Undo ו־Redo משחזרים את התוכן הצפוי. שמירה מושהית או בקשה שכבר יצאה אינה מחזירה תוכן שבוטל. שינוי תוכן אינו משאיר אימות ישן כתקף, ואינו משנה גרסאות מאושרות.

**אימות למסירה:** ביטול לפני debounce ואחרי שמירה, Redo, עריכה חדשה אחרי Undo וקונפליקט. להרחיב כיסוי קיים רק לתרחישים מהותיים שחסרים בו.

### UX-03 — עריכת הגדרות התאמה במועמדות קיימת

- [x] **השלמת המשימה**

**מטרה:** לאפשר שינוי מסלול, פרופיל ודגשים לאחר פתיחת המועמדות, כאשר חוזה הפעולות מתיר זאת.

**מצב קיים:** שדות החלטה למסלול, פרופיל ודגשים כבר קיימים בממשק הסקירה. צריך לבדוק באילו מצבים הם נגישים; נעילה לצמיתות אינה מסקנה מאומתת.

**מפרט מחייב:** `docs/spec/state-and-use-cases.md`, סעיפים 3–6, 9 ו־13. `apply_analysis_decisions` יוצר Analysis או SelectionPlan חדשים לפי סוג השינוי; אין להניח שכל שינוי דורש ניתוח מחדש.

**אזורי עבודה:** `VerificationStage`, `ReviewDecisionPanel`, `ReviewDecisionForm`, `JobSnapshotPanel` ושכבת ה־API של ניתוח והחלטות.

**צעדים לביצוע:**

- לבדוק הרשאות ונגישות במצבי ניתוח, טיוטה, אישור ו־Ready.
- לבחור בפקודה הקיימת המתאימה. אם חסר חוזה נדרש, לתאר את הפער לפני מימוש.
- להוסיף כניסה ברורה בשם „ערוך הגדרות התאמה”, עם הערכים הנוכחיים וזיהוי שינויים.
- להסביר לפני השמירה השלכות רלוונטיות: הקשר פעיל חדש, טיוטה שאינה עדכנית או צורך בהחלפתה.
- לרענן פרטי מועמדות לאחר הצלחה ולהציג את הצעד הבא מתוך מצב השרת.

**תנאי סיום:** שינוי אפשרי במצבים המותרים וסירוב מוצג עם סיבה. נוצרות הרשומות החדשות הנדרשות בלי שינוי היסטוריה. גרסה קודמת שעדיין `ready_qualified` נשארת נגישה, עם הקשר היסטורי כשנדרש.

**אימות למסירה:** שינוי סיווג לעומת בחירה בלבד, טיוטה קיימת, Ready היסטורי וסירוב לפי הרשאות. שינוי במשמעות ערך, חתימה ציבורית או projection מחייב גם pipeline דטרמיניסטי ללא AI.

### UX-04 — השלמת חוויית ביטול וניסיון חוזר

- [ ] **בדיקת הפערים והשלמת המשימה**

**מטרה:** להציג מה קורה בפעולת רקע ואיך אפשר להתקדם לאחר ביטול או כישלון.

**מצב קיים:** `ActiveOperationPanel` כבר משתמש ב־`OperationActions`, שמפעיל `cancelOperation` ו־`retryOperation` לפי `available_actions` מהשרת. ביטול נחשף לאחר ארבע שניות בפעולה פעילה; תיאורי התקדמות וכישלון כבר קיימים.

**בירור ומימוש ממתינים לאימות:** המעקב אחרי הרשומה האחרונה, המעבר ל־Operation חדש ב־Retry, ההפרדה בין בקשת ביטול לביטול סופי, נעילת לחיצה כפולה והנחיה כש־Retry חסום כבר ממומשים. נוסף להם כיסוי רגרסיה ממוקד; שערי המשתמש טרם הורצו.

**מפרט מחייב:** `docs/spec/state-and-use-cases.md`, סעיפים 11 ו־19.

**אזורי עבודה:** `ActiveOperationPanel`, `OperationActions`, `operationProgress`, `operationLabels` וכל מסך שמציג Operation.

**צעדים לביצוע:**

- לבדוק ניתוח, יצירת טיוטה ורינדור ולרשום היכן חסרה תצוגה או פעולה שכבר מותרת בשרת.
- להשלים שלבים קריאים מתוך `phase` והודעות השרת; לא להמציא אחוזי התקדמות.
- לוודא ש־Retry זמין אחרי כישלון כשמותר, וכשחסום מוצגת סיבה או דרך פתרון מתאימה.
- לוודא מעקב אחר ה־Operation החדש וכישלון גלוי גם כשאין עוד `active_operation`.
- להבחין בין בקשת ביטול שהתקבלה לביטול שהושלם ולמנוע לחיצות כפולות בזמן בקשה.

**תנאי סיום:** הפעולות נגישות בכל נקודות התצוגה הרלוונטיות לפי הרשאות השרת. Retry יוצר Operation חדש ושומר את הקודם. תוצאה שהופקה אחרי בקשת ביטול אינה מופעלת.

**אימות למסירה:** queued, running, בקשת ביטול, cancelled, failed, interrupted, Retry מותר וחסום ומעבר למעקב החדש. להתחיל מ־`ActiveOperationPanel.test.tsx` ומ־`api/queryPolicy.test.ts`.

### UX-05 — עדכון מצב הטיוטה לאחר אישור עובדה

- [x] **השלמת המשימה**

**מטרה:** אחרי „אשר והשתמש בעובדה”, להציג את מצב הטיוטה העדכני ואת הצעד הבא בלי רענון ידני.

**בירור מול הקוד:** UX-11 כבר כיסתה שמירת עריכות לפני פתרון, רענון מועמדות/טיוטה וחסימת אישור בעקבות החלפת הקשר. `confirmAndUseFact` מחזירה את העובדה, תוכנית הבחירה החדשה ונתוני האירועים/גרסאות; השרת אינו מעדכן או מחבר מחדש את הטיוטה הקיימת. במסלול הנוכחי החלפת התוכנית הופכת אותה ל־stale. תגי השורות מתארים ביסוס, ולא תוצאת אימות.

**התיקון שנכתב:** רענון מפורש שממתין למועמדות, לטיוטה ולעובדותיה, לאחר שמירת עריכות שנעשו בזמן הפעולה. מוצגים staleness ופעולת תיקון לפי השרת; רק כשהשרת מתיר `validate` מופעל אימות לגרסה המדויקת שנקראה אחרי השמירה. תוצאות ישנות מוסתרות בזמן המעבר, בעריכות מקומיות ובאי־התאמת גרסה/hash. תג שורה אינו משתנה בעקבות אישור עובדה בלבד. כשל ברענון או באימות מוצג בנפרד מאישור העובדה שהצליח; ניסיון נוסף אינו מאשר את העובדה מחדש. אישור גרסה נשאר בחירה מפורשת וחסמי השרת נשמרים.

**מצב אימות:** לפי דיווח המשתמש, בדיקת הטיפוסים, lint הממוקד ובדיקות `DraftEditorPage.test.tsx` עברו לאחר תיקון ה־mock והסרת הפרמטר המיותר. `DraftApprovalFlow.test.tsx` ו־`useDraftAutosave.test.ts` עברו בריצה הקודמת; תיקוני ה־mock והפרמטר בלבד לא חייבו הרצה חוזרת שלהן. סך הכיסוי בשלוש החבילות נשאר 56 תרחישים, כולל שבעה תרחישי UX-05 שנוספו. הכשלים בריצה הראשונה נפתרו, ולא נותר שער פתוח למסירת UX-05. אין שינוי בקאנד, חתימת API, projection או ארטיפקטים.

**מפרט מחייב:** `docs/spec/state-and-use-cases.md`, סעיפים 5–7 וחוזי בחירה, אימות ואישור עובדות.

**אזורי עבודה:** `ClaimFactResolution`, שאילתות טיוטה ואימות, `useDraftEditing` ותגי מצב השורות.

**צעדים לביצוע:**

- לבדוק מה חוזר מ־`confirmAndUseFact` ומה קורה לטיוטה הפעילה אחרי החלפת התוכנית.
- להשלים רענון טיוטה ותוצאת אימות רלוונטית ולמנוע הצגת תוצאה ישנה בזמן המעבר.
- אם הטיוטה כשירה לאימות, להפעיל אימות אוטומטי לפי החוזה ועל הגרסה המדויקת, אחרי התיישבות עריכות מקומיות.
- אם הטיוטה נעשתה stale, להציג סיבה ופעולת תיקון מותרת במקום לסמן אותה כמאומתת.
- להפריד כשל ברענון או באימות מאישור עובדה שכבר הצליח.

**תנאי סיום:** המסך מתעדכן ללא F5. תג שורה משתנה ל„מאומתת” רק בעקבות אימות תקף; אישור עובדה לבדו אינו מסיר חוסמי אישור. תוצאה מאוחרת של גרסה ישנה אינה גוברת על עריכה חדשה.

**אימות למסירה:** אישור עם שינוי תוכנית, טיוטה stale, אימות שעובר או נכשל, עריכה בזמן הפעולה וכשל ברענון. שינוי חוזה אימות או projection מחייב גם pipeline דטרמיניסטי ללא AI.

### UX-06 — שחזור הקשר מרענון וקישור ישיר

- [x] **השלמת המשימה**

**מטרה:** כניסה לאותה כתובת בטאב חדש או אחרי רענון תציג את מצב העבודה הנכון ותאפשר להמשיך.

**מצב קיים:** `ApplicationPage` משתמש ב־`location.state.createdApplication` למידע על בקשת היצירה ותזמון הניתוח. רוב מצב המועמדות כבר נטען מהשרת. לא נמצא שימוש ב־`ApplicationDetail.preparation_progress`; אין לבסס את העבודה על שדה שאינו קיים בחוזה הנוכחי.

**אומת בקוד ובכיסוי הקיים:** רוב ההקשר כבר נקרא מהשרת; route state משנה חיווי טעינה והודעת יצירה בלבד. בדיקות קיימות מכסות פעולה פעילה, החלטות inline, טיוטה, Ready, שימור טקסט בכשל שמירה וקונפליקט וחסמי אישור. הפער הוא שחזור Operation שכבר הסתיים: המעקב קרא רק `active_operation`, אף שהחוזה מספק גם `latest_operation`.

**בוצע:** שחזור מה־Operation הפעיל או האחרון ובידוד המעקב לפי מזהה המועמדות בזמן הרינדור. פעולות פעילות קובעות את מסך הכניסה גם לצד Ready. המשך אוטומטי דורש תוצאות ניתוח ותוכנית שהופעלו ותואמות למזהים הפעילים, הגדרת אוטומציה ופעולת `create_draft` מותרת, בלי החלטות פתוחות או טיוטה קיימת. מפתח האידמפוטנטיות הקיים נשמר ברענון; תוצאה מאוחרת ממועמדות אחרת אינה פותחת מעקב או ניווט. ניווט לעורך דורש טיוטה מדויקת שהופעלה בהקשר הנוכחי; סימון ניווט שהושלם אינו מקפיץ שוב ב־Back/Forward. הודעת כשל תזמון מהיצירה נעלמת כשמצב השרת התקדם. ניתוח היסטורי אינו מסתיר הקשר חדש או משאיר תצלום שנוצר אחריו בהמתנה.

**גבול:** מנגנון ה־sessionStorage וסימוני ההמשך הקיימים נשמרו. אחסון חסום, החלפת מנגנון ההמשך והרחבת חוזה השרת שייכים ל־UX-21 ולא בוצעו. ה־buffer המקומי של העורך נשמר, ונוסף כיסוי לשחזורו מול רענון נתוני שרת.

**מצב האימות:** בהרצה החוזרת של המשתמש עברו כל 83 הבדיקות הממוקדות. גם `lint:strict` עבר לאחר תיקון שלוש השגיאות והאזהרה שנמצאו בהרצה הקודמת.

**אזורי עבודה:** `ApplicationPage`, `analysisViewState`, `useAutomaticDraft`, `useWatchedOperation`, `workflowStages` וניתוב שלבי ההכנה.

**צעדים לביצוע:**

- למפות החלטות זרימה שתלויות במידע זמני מניווט ולהפריד אותן מהודעות זמניות על בקשת יצירה.
- לבסס שחזור שלב ופעולה על ה־URL ועל שדות ApplicationDetail הקיימים: מצבי הכנה וטיוטה, מזהי הקשר פעיל, Operations ופעולות מותרות.
- לוודא שהיעדר route state אינו מסתיר ניתוח פעיל, כישלון או דרך להמשך.
- לוודא שרענון או חזרה אינם מתזמנים פעולה אוטומטית כפולה ואינם עוקפים החלטה שנדרשת מהמשתמש.

**תנאי סיום:** F5, קישור ישיר, טאב חדש ו־Back / Forward מאפשרים להמשיך מהמצב השמור. ההקשר נשען על מזהים מפורשים מהשרת; מידע זמני מהיצירה אינו תנאי להמשך.

**אימות למסירה:** כניסה ללא route state במצבי needs_analysis, פעולה פעילה, כישלון, החלטה פתוחה, טיוטה ו־Ready; גם יצירה שהצליחה אך תזמון הניתוח נכשל.

### UX-12 — שינוי סדר סעיפים ושורות בטיוטה

- [ ] **השלמת המשימה**

**מטרה:** להבליט סעיפים והישגים רלוונטיים בלי למחוק ולהקליד שורות מחדש.

**מצב קיים:** `DraftOutlineEditor` ו־`DraftSectionCard` מציגים את סדר המערכים שסופק בטיוטה. ברכיבים שנבדקו אין פקדי הזזה; צריך לבדוק אם חוזה העדכון והמנוע כבר תומכים בשימור סדר משתמש.

**מפרט מחייב:** `docs/spec/state-and-use-cases.md`, חוזי עדכון טיוטה ואימות; `docs/spec/architecture.md` ו־`docs/spec/product-spec.md` למבנה המסמך ולגבולות העובדות.

**אזורי עבודה:** `DraftOutlineEditor`, `DraftSectionCard`, `DraftClaimRow`, `useDraftEditing`, `useDraftAutosave`, חוזה עדכון הטיוטה וה־renderer.

**צעדים לביצוע:**

- להגדיר אילו סעיפים ושורות ניתנים להזזה. להשאיר זהות ופרטי קשר במקומם כשמבנה המסמך מחייב זאת.
- להוסיף Move Up / Move Down ברמת סעיף וברמת שורה בתוך הסעיף, עם השבתה בקצוות ושמות נגישים.
- לשמר מזהי שורות, שיוך עובדות, כותרות משרה, תאריכים והקשר ניסיון. שינוי סדר אינו העברת הישג למעסיק אחר.
- לשמור את הסדר דרך חוזה היישום וה־API, עם ETag ותור שמירה קיים. אם החוזה אינו תומך בכך, להגדיר את ההרחבה לפני מימוש UI.
- לוודא שזוזה אינה נדרסת ב־autosave וש־HTML/PDF מכבדים את הסדר השמור. לתאם עם היסטוריית UX-02 אם ממומשת.

**תנאי סיום:** הסדר שורד שמירה וטעינה מחדש ומופיע בתצוגה וברינדור. אין שורות כפולות או חסרות, אין שינוי משמעות או שיוך עובדות, ואימות ישן אינו מוצג כתקף לאחר שינוי מסמך.

**אימות למסירה:** הזזה בקצוות, סעיף עם שורה אחת, שמירה במקביל לעריכה, קונפליקט ושימור שיוך ניסיון. שינוי rendering מחייב golden ודפדפן; שינוי חתימה ציבורית או משמעות הסדר השמור מחייב גם pipeline ללא AI. שינויי האש צפויים רק בתרחישים שהסדר בהם שונה בכוונה.

### UX-13 — היסטוריית נוסחי משרה והשוואת תצלומים

- [ ] **השלמת המשימה**

**מטרה:** להבין מה השתנה במודעה ומדוע ניתוח או טיוטה קודמים כבר אינם מתאימים להקשר הפעיל.

**מצב קיים:** `JobSnapshotPanel` מציג `detail.latest_snapshot`; אין בו בורר היסטוריה או Diff. יש לבדוק זמינות שאילתות היסטוריה ותוכן התצלומים ב־API.

**אזורי עבודה:** `JobSnapshotPanel`, `JobPostingUpdate`, שאילתות מועמדות ו־JobSnapshot, ורכיב השוואת טקסט.

**צעדים לביצוע:**

- להוסיף „היסטוריית נוסחי משרה” עם מספר גרסה, מועד שמירה וסימון התצלום הפעיל.
- לאפשר בחירת שני תצלומים לפי מזהים מפורשים; ברירת המחדל תשווה את האחרון לקודמו אם קיים.
- להציג Inline Diff או Side-by-side, עם תוספות בירוק ומחיקות באדום וגם סימון שאינו נשען על צבע בלבד.
- להשוות טקסט שמור בדיוק, כולל רווחים ושבירות שורה; להציג שינוי URL בנפרד משינוי תוכן.
- להציג טקסט כטקסט, ללא הרצת HTML, ולתמוך בעברית, באנגלית ובתוכן גדול. תוכן שלא ניתן לקרוא יוצג כחסר ולא ישוחזר מהמודעה החיה.

**תנאי סיום:** כל זוג תצלומים זמין ניתן להשוואה, והמשתמש מזהה תוספות ומחיקות. ההשוואה אינה משנה תצלומים או מחליפה את ההקשר הפעיל; מודעה שנעלמה מהאינטרנט עדיין ניתנת לקריאה מהמקור השמור.

**אימות למסירה:** תצלום יחיד, שניים ויותר, טקסט זהה עם URL שונה, שינויים ברווחים, RTL, תוכן גדול וכשל טעינה. קשור ל־UX-09, שעוסק בטופס עדכון ולא בהיסטוריה. הרחבת חתימה או projection מחייבת pipeline ללא AI.

### UX-14 — אזור מרוכז לפרטי מועמד ויצירת קשר

- [ ] **בירור גבולות המימוש והשלמת המשימה**

**מטרה:** למצוא ולעדכן שם ופרטי קשר במקום מרוכז, באמצעות מחזור חיי העובדות הקיים.

**מצב קיים:** לפי הארכיטקטורה, CandidateContext מפנה לעובדות קנוניות של זהות וקשר; `base/candidate.json` קיים. צריך למפות את המקורות וההפניות בפועל, בלי להניח שכל הערכים נשמרים בקובץ הזה.

**גבול מפרט מפורש:** `docs/spec/product-spec.md`, סעיפים 3–5, 7 ו־17, אינו מאפשר candidate CRUD או עריכה כללית של Knowledge. תיקון עובדה קנונית נעשה באמצעות עובדה חדשה עם `replaces`, ולא בשינוי המקור הישן. אזור שמציג את העובדות ומפשט את התיקון יכול להשתמש בחוזה הקיים; עריכת `candidate.json` או CandidateContext מה־Web מחייבת קודם שינוי מפרט מאושר.

**אזורי עבודה:** `FactsPage` או `SettingsPage`, רכיבי יצירה ותיקון עובדות, שאילתות העובדות והצגת CandidateContext.

**צעדים לביצוע:**

- למפות שם, טלפון, אימייל, LinkedIn, GitHub ועיר לעובדות שהמערכת באמת נושאת; שדה חסר נשאר חסר.
- להוסיף אזור „פרטי מועמד ויצירת קשר”, עם ערך נוכחי, מקור וקישור לעובדה.
- לפשט את יצירת תיקון מתוך השדה, תוך שימור `replaces`, אישור מפורש וקידום לפי החוזה הקיים.
- לבדוק כיצד התיקון מעדכן את ההפניה הקנונית. אם אין פקודה מורשית לכך, לתעד את החסם ולא לעקוף אותו בעריכת קובץ ישירה.
- להציג השלכות על טיוטות תלויות, בלי לשנות גרסאות או פרטי קשר שכבר נכללו בארטיפקטים היסטוריים.

**תנאי סיום:** הפרטים הקיימים מוצגים במקום אחד, ותיקון נתמך ניתן לביצוע בלי חיפוש ידני. נשאר מקור קנוני אחד לכל פרט, עם היסטוריית תיקון ומצבי עובדה גלויים. אין מאגר פרטי קשר מקביל.

**אימות למסירה:** פרט חסר, תיקון טלפון או אימייל, URL שגוי, ביטול תיקון וכשל קידום. שינוי הפניה או משמעות ערך שמור מחייב pipeline ללא AI; שינוי רינדור מחייב גם golden ודפדפן.

### UX-15 — פעולות מרוכזות ודפדוף במאגר העובדות

- [ ] **השלמת המשימה**

**מטרה:** לנהל קבוצת עובדות בלי מחזור פתיחה ופעולה נפרד לכל עובדה, ולשמור רשימה נוחה כשהמאגר גדל.

**מצב קיים:** `FactPoolList` מקבץ את הרשומות ומרנדר את כולן באזור גלילה; אין בו בחירה קבוצתית או דפדוף. לבדוק את נפח הנתונים ואת חוזי הפעולות לפני בחירת פתרון טעינה.

**מפרט מחייב:** `docs/spec/state-and-use-cases.md`, סעיף 17 וחוזי מחיקה קיימים; `docs/spec/product-spec.md`, מחזור חיי Knowledge; `docs/spec/architecture.md`, גבולות שכבות ויומן מוטציות.

**אזורי עבודה:** `FactsPage`, `FactPoolList`, פעולות אישור, קידום ושיוך, שאילתות רשימה ושירותי היישום.

**צעדים לביצוע:**

- להוסיף checkbox לכל עובדה וסרגל עם מספר הנבחרות; להגדיר אם „בחר הכול” מתייחס לעמוד הנוכחי או לכל תוצאות המסנן.
- להוסיף אישור קבוצתי, קידום קבוצתי ושיוך ל־Profile ולסעיף קיימים, לפי הכשירות של כל עובדה. מחיקה קבוצתית אינה חלק מהשלב הראשון.
- להציג לפני ביצוע את העובדות והפעולה המדויקת. מעבר pending → confirmed → canonical נשאר מפורש ומתועד גם בקבוצה.
- להגדיר חוזה לכשל חלקי: תוצאה לכל עובדה, שימור הצלחות וניסיון חוזר רק לנכשלות כשמותר. אם נדרשת אטומיות לקבוצה, לממש בשכבת היישום ולא להניח שלולאת בקשות מבטיחה אותה.
- לבחור דפדוף תוצאות או טעינה מדורגת לפי הצורך. Virtualization מפחית רינדור בלבד ואינו תחליף לפגינציה בשרת; להוסיף אותו רק אם יש צורך מוכח.
- לשמר בחירה, פוקוס ומצב המסננים באופן מוגדר, ולהימנע מאישור אוטומטי של עובדות שנוספו אחרי הבחירה.

**תנאי סיום:** אפשר לאשר, לקדם ולשייך קבוצה כשירה עם תוצאה ברורה לכל עובדה. טעינת הרשימה ובחירת „הכול” אינן מטעות לגבי ההיקף. אין דילוג על אישור, שינוי עובדות קנוניות במקום או עריכת הגדרות Profile.

**אימות למסירה:** קבוצה של 20–30 עובדות, מצבים מעורבים, כשל חלקי, לחיצה חוזרת, שינוי מסנן ופעולה על רשומה שהשתנתה. הרחבת API או projection מחייבת pipeline ללא AI. אין להוסיף מבחן ביצועים כבד בלי צורך שנמצא.

### UX-16 — טעינה, שגיאה וניסיון חוזר ב־ApplicationResumePage

- [x] **השלמת המשימה**

**מטרה:** כניסה מקישור ישיר לא תישאר בהמתנה בלתי מוסברת אחרי כשל טעינה.

**מצב קיים:** `ApplicationResumePage` אכן מעביר `loading` קבוע. עם זאת, `QueryState` כבר מחזיר `errorState` לפני תוכן הטעינה, וגם מטפל ב־404 בנפרד. לכן טענת הספינר האינסופי עקב הקדימות אינה מתאימה לקוד הנוכחי; צריך לשחזר כשל לפני רישום באג מאומת.

**ממצא לאחר סקירת הכיסוי:** שתי הבדיקות הקיימות בדקו יעדי טיוטה ו־Ready בלבד. קדימות השגיאה ו־404 כבר קיימת ב־`QueryState`; טענת הספינר האינסופי לא אושרה בקוד, ולא בוצע שחזור ריצה כי המשתמש מריץ את הבדיקות. הפער המאומת הוא היעדר Retry מפורש אחרי כשל סופי.

**בוצע:** חיווי טעינה לפי מצב השאילתה, Retry שמבצע קריאה חוזרת לאותו מזהה וננעל בזמן הבקשה, ושמירת מסך 404. הכניסה ממתינה לתשובת שרת עדכנית במקום להפנות מתוך מטמון ישן. נוספו תרחישי כשל רשת ו־500 עם מדיניות ה־retry האוטומטי הקיימת (ניסיון חוזר אחד), התאוששות ללא POST, 404 ומטמון משלב ישן.

**מצב האימות:** בהרצה החוזרת של המשתמש עברו כל 83 הבדיקות הממוקדות, כולל כשל רשת, 500, Retry ו־404. גם `lint:strict` עבר.

**אזורי עבודה:** `ApplicationResumePage.tsx`, `ApplicationResumePage.test.tsx`, `QueryState` ושאילתת פרטי המועמדות.

**צעדים לביצוע:**

- לבדוק כשל רשת, 500 ו־404 בכניסה ללא נתונים שמורים ולהתייחס למדיניות ה־retry האוטומטית הקיימת.
- להחליף את החיווי הקבוע ב־`loading={query.isPending}` כדי לבטא את מצב השאילתה במפורש, בלי לשנות את קדימות השגיאה ברכיב המשותף.
- לבדוק אם קיימת דרך לנסות שוב לאחר כשל סופי; אם חסרה, להוסיף פעולה שמבצעת refetch ושומרת על אותה מועמדות.
- לשמר ניתוב ליעד הנכון לאחר הצלחה ואת מסך „לא נמצא” ב־404.

**תנאי סיום:** בזמן טעינה מוצג חיווי; אחרי כשל סופי מוצגת שגיאה עם דרך התאוששות מתאימה. ניסיון חוזר מוצלח מוביל לשלב הפעיל בלי ליצור מועמדות או Operation נוספים.

**אימות למסירה:** לבדוק קודם את הכיסוי ב־`ApplicationResumePage.test.tsx`. להרחיבו רק אם אינו מכסה את הכשל או ההתאוששות שנמצאו; לתעד אם הממצא המקורי לא שוחזר.

### UX-19 — אימות ההקשר בייצוא תיעוד החלטות

- [ ] **אימות הממצא והשלמת פערים אם נמצאו**

**מטרה:** ייצוא Markdown יציג את החברה, התפקיד וההחלטות של הגרסה שנבחרה, בלי תוויות גנריות שהומצאו במקום מידע חסר.

**מצב קיים:** `mockDispatcher.ts` לא נמצא בעץ שנבדק. בזרימה האמיתית `useRevisionData` מפעיל את שאילתת הייצוא רק אחרי קבלת `revision.application_id`, והלקוח מעביר אותו ב־Query Parameter. ה־router מקבל את המזהה והשירות מאמת את שיוך המועמדות, הגרסה ורשומת ההחלטות. לכן העברת מחרוזת ריקה ב־mock אינה באג מאומת במימוש הנוכחי.

**מפרט מחייב:** `docs/spec/state-and-use-cases.md`, חוזה `export_decision_markdown`; `docs/spec/product-spec.md`, כללי provenance והיסטוריה בלתי משתנה.

**אזורי עבודה:** `features/revisions/api/queries.ts`, `api/revisions.ts`, `api/routers/approved_revisions.py`, `application/services/drafts/decisions.py` ובדיקות ייצוא קיימות.

**צעדים לביצוע:**

- לבדוק את הבקשה והמסמך עבור מועמדות עם חברה, תפקיד ופרופיל ידועים, כולל גרסה היסטורית שאינה ההקשר הפעיל.
- לוודא שאין בקשה לפני שהמזהה זמין ושמפתח המטמון כולל את המועמדות והגרסה.
- לוודא שסיווג, דגשים ופערים מגיעים מרשומת ההחלטות של הגרסה, ולא מהניתוח הפעיל החדש.
- אם נמצא mock רלוונטי, לתקן אותו לפי חוזה ה־API, כולל סירוב לשיוך שגוי; לא לשנות חתימה רק כדי להתאים ל־mock.
- מידע שלא נרשם בעבר נשאר חסר. לא להוסיף בדיעבד פרופיל, החלטה או ערך ברירת מחדל לרשומות היסטוריות.

**תנאי סיום:** ההקשר הנכון מופיע בייצוא, ומזהה חסר או שיוך שגוי אינם מפיקים מסמך הצלחה גנרי. אם לא נמצא פער, לסגור עם הפניה לכיסוי הקיים ולציין שהממצא המקורי לא שוחזר.

**אימות למסירה:** ייצוא רגיל, גרסה היסטורית, מזהה חסר ושיוך שגוי. לבדוק קודם כיסוי שירות/API ו־`api/queryPolicy.test.ts`; להוסיף רגרסיה רק לחסר מהותי. שינוי rendering של מסמך הייצוא מחייב golden ודפדפן לפי שער הפרויקט, ושינוי חתימה ציבורית של היישום/API מחייב pipeline ללא AI.

### UX-20 — התאוששות מקונפליקט ETag בהגדרות

- [ ] **השלמת המשימה**

**מטרה:** אחרי שינוי הגדרות בחלון אחר, לאפשר המשך שמירה בלי רענון עמוד ואובדן עריכות מקומיות.

**מצב קיים:** `SettingsForm` שולח את ה־ETag שקיבל ומציג `ErrorCallout` בכשל. אין טיפול ייעודי ב־412 או פעולת השוואה ורענון. הכפתור אינו ננעל בקוד בגלל 412 בלבד; יש לבדוק כיצד refetch ועדכון `values` משפיעים על העריכות בטופס. שינוי אוטומטי של תעריפים כגורם לקונפליקט לא אומת.

**מפרט מחייב:** `docs/spec/state-and-use-cases.md`, סעיף 19a. קונפליקט אינו משנה דבר בשרת, ושמירה חדשה חייבת להתייחס לגרסה עדכנית במפורש.

**אזורי עבודה:** `SettingsForm`, `SettingsPage`, `api/settings.ts`, `settings.model.ts` ובדיקות ההגדרות.

**צעדים לביצוע:**

- לזהות 412 ולהציג „ההגדרות השתנו מאז שפתחת את הטופס”, עם פעולה ייעודית לפתרון.
- לשמור בזיכרון את ערכי הבסיס ואת העריכות המקומיות לפני קריאת ההגדרות וה־ETag החדשים; לא לאפס את הטופס עקב רענון מטמון.
- להציג הבדלים בין הבסיס, הערכים העדכניים והעריכות. לאפשר טעינת ערכי השרת תוך השלכה מפורשת של המקומיים, או החלת עריכות נבחרות על הגרסה החדשה.
- לא לרענן רק ETag ולשלוח אוטומטית את כל הטופס הישן: פעולה כזו יכולה למחוק שינוי מקביל. גם שמירת בחירה מפורשת משתמשת ב־If-Match עדכני.
- לטפל בקונפליקט נוסף בזמן הפתרון ובכשל רענון בלי לאבד עריכות, ולסנכרן את תצוגת ההעדפות לאחר פתרון או שמירה.

**תנאי סיום:** שני חלונות יכולים ליצור קונפליקט, לראות את ההבדלים ולהגיע לשמירה מוצלחת בלי F5 ובלי דריסה שקטה. שדות שהמשתמש לא שינה נשמרים מהגרסה העדכנית, לפי בחירת הפתרון.

**אימות למסירה:** שני חלונות, שינויים באותו שדה ובשדות שונים, כשל refetch, קונפליקט חוזר והשלכה מפורשת. להתחיל מ־`SettingsPage.test.tsx`. שינוי חוזה עדכון או משמעות ערך מחייב pipeline ללא AI.

## P2 — שיפור

### UX-07 — העתקת תוכן כטקסט / Markdown

- [ ] **השלמת המשימה**

**מטרה:** להעתיק קורות חיים לטפסי מועמדות בלי העתקה ידנית מתוך תצוגת המסמך.

**אזורי עבודה:** `DraftWorkspace`, `RevisionPage` ומקור התוכן המובנה של כל מסך.

**צעדים לביצוע:**

- להוסיף „העתק תוכן קורות חיים” עם בחירה בין טקסט רגיל ל־Markdown.
- להגדיר מקור מדויק: במסך גרסה — הגרסה המוצגת; בטיוטה — להחליט ולציין אם מעתיקים תוכן מקומי או שמור.
- לשמר סדר סעיפים, כותרות, תבליטים ושפות; לא לכלול בקרות ממשק או תגי אימות.
- להציג הצלחה וכישלון של Clipboard API בצורה נגישה.

**תנאי סיום:** ההדבקה מכילה את התוכן הנבחר בסדר נכון ובפורמט קריא, בלי הוספת עובדות או ערבוב בין טיוטה לגרסה מאושרת.

**אימות למסירה:** שני הפורמטים, RTL, סעיפים ריקים וכשל Clipboard. לבדוק הדבקה בשדה רגיל ובטופס מייצג, כגון Greenhouse, Comeet, Workday או LinkedIn.

### UX-08 — הרחבת החיפוש הגלובלי

- [ ] **השלמת המשימה**

**מטרה:** להגיע לתוצאות שמעבר למעבר המהיר ולברר הרחבה לעובדות ולטיוטות היסטוריות.

**מצב קיים:** `GlobalSearchDialog` מחפש מועמדויות דרך השרת ומבקש עד שמונה תוצאות. חיפוש ריק מציג „דורש טיפול”. הסרת `limit: 8` לבדה אינה מבטיחה גישה לכל התוצאות.

**אזורי עבודה:** `GlobalSearchDialog`, רשימת המועמדויות ושאילתות API קיימות; חוזים נוספים רק אם נדרשים.

**צעדים לביצוע:**

- לשמור רשימת מעבר מהיר מוגבלת ולהוסיף „כל התוצאות”, עם העברת המונח לרשימה שתומכת בדפדוף תוצאות.
- לבדוק שאילתות לחיפוש עובדות והיסטוריה. אם חסרות, להגדיר חוזה יישום ו־API לפני הרחבת הממשק.
- להציג סוג תוצאה והקשר ולקשר למזהה המדויק, בלי להחליף רשומה היסטורית בגרסה האחרונה.
- לטפל בטעינה, אין תוצאות וכישלון ולשמר ניווט במקלדת והתנהגות חיפוש ריק.
- לכבד החרגת רשומות מחוקות מחיפוש ברירת המחדל.

**תנאי סיום:** יותר משמונה התאמות נגישות דרך „כל התוצאות”. בהרחבה לעובדות ולהיסטוריה, כל סוג מסומן ומוביל לרשומה הנכונה דרך ה־API.

**אימות למסירה:** יותר משמונה תוצאות, שימור מונח, עברית ואנגלית, רשומות מחוקות ומקלדת. הרחבת חתימה ציבורית או projection מחייבת גם pipeline דטרמיניסטי ללא AI.

### UX-09 — ולידציה וזיהוי שינויים בעדכון משרה

- [ ] **השלמת המשימה**

**מטרה:** למנוע URL שגוי או עדכון ללא שינוי ולהבהיר ששמירה יוצרת JobSnapshot חדש.

**מצב קיים:** `JobPostingUpdate` טוען טקסט ו־URL קיימים ושולח יצירת Snapshot חדש. הטקסט נשמר ללא trim; ולידציית URL וזיהוי הבדלים דורשים בדיקה והשלמה.

**אזורי עבודה:** `JobPostingUpdate`, ולידציית intake משותפת וחוזה `create_job_snapshot`.

**צעדים לביצוע:**

- למחזר ולידציית URL קיימת כשמתאימה, בהתאם לחוזה השרת; לאפשר שדה ריק כאשר אופציונלי.
- להשוות טקסט ו־URL למקור לפי כללי השמירה הקיימים. לא לנרמל או להסיר רווחים מטקסט המשרה.
- למנוע שליחה ללא שינוי ולהציג שגיאת שדה לפני בקשה כשאפשר לזהות אותה בלקוח.
- להסביר שה־Snapshot הקודם נשמר ושעדכון ההקשר עשוי לחייב ניתוח חדש ולהפוך טיוטה ללא עדכנית.
- לשמור קלט אחרי כישלון ולהציג סירוב שרת, כולל כפילות או מצב שאינו מאפשר עדכון.

**תנאי סיום:** URL שגוי ושמירה ללא שינוי מטופלים בממשק. שינוי תקף יוצר Snapshot חדש עם הטקסט המדויק; הקודם נשמר וההקשר החדש מוצג לאחר הצלחה.

**אימות למסירה:** URL תקין, שגוי וריק; ללא שינוי; שינוי URL בלבד; שינוי טקסט בלבד, כולל רווחים; כשל שרת ורענון הקשר.

### UX-10 — אחידות דיאלוגים וניהול פוקוס

- [ ] **מיפוי החריגים והשלמת המשימה**

**מטרה:** פתיחה, סגירה וניווט במקלדת עקביים בדיאלוגים, תוך שמירת התנהגות ייעודית.

**מצב קיים:** `frontend/src/ui/Dialog.tsx` כבר משתמש ב־`<dialog>` טבעי עם ניהול פוקוס ו־`dismissible`. החיפוש משתמש בדיאלוג טבעי נפרד עם combobox. `ClaimFactResolution` הוא אזור פתרון בתוך הטיוטה, ואינו כשלעצמו דיאלוג.

**אזורי עבודה:** `ui/Dialog`, `GlobalSearchDialog` ודיאלוגי מועמדות, עובדות, קונפליקט, אישור והגשה.

**צעדים לביצוע:**

- למפות דיאלוגים אמיתיים בחיפוש שנגזר מהקוד; לא להפוך אזור רגיל למודל בגלל שם הרכיב.
- להעביר לרכיב המשותף שימושים שמתאימים לו. בחיפוש לשמר פוקוס התחלתי בשדה ו־combobox באמצעות התאמה או חריג מנומק.
- לבדוק focus trap, פוקוס התחלתי, חזרה למפעיל, שם נגיש וגלילה במסך קטן.
- לשמר Escape וסגירה ברקע לפי המדיניות; דיאלוג שאינו dismissible דורש בחירה מפורשת.
- לבדוק שסגירת דיאלוג פנימי אינה סוגרת את ההורה. להוסיף תלות רק לצורך חוזי שהמנגנון הקיים אינו פותר.

**תנאי סיום:** התנהגות עקבית או חריגה ייעודית מתועדת. כל תרחיש ניתן לביצוע במקלדת; הפוקוס חוזר ליעד מתאים בלי אישור או השלכת תוכן בסגירה לא מכוונת.

**אימות למסירה:** Tab / Shift+Tab, Escape מותר וחסום, רקע, חזרה למפעיל, חיפוש במקלדת, דיאלוג פנימי ו־RTL. להתחיל מהכיסוי הקיים.

### UX-17 — היסטוריית גרסאות מאושרות והשוואת תוכן

- [ ] **השלמת המשימה**

**מטרה:** לעבור בין גרסאות של אותה מועמדות, להשוות נוסחים ולזהות את הגרסה והקובץ שנמסרו בכל הגשה.

**מצב קיים:** `RevisionPage` פונה לגרסה מפורשת דרך ה־URL ומציג אותה. אין בו בורר גרסאות; צריך לבדוק אילו נתוני היסטוריה והגשה זמינים כבר בשאילתות.

**אזורי עבודה:** `RevisionPage`, `RevisionSummary`, שאילתות revisions ומועמדות, היסטוריית Submission וקישורי הורדת PDF.

**צעדים לביצוע:**

- להוסיף בורר גרסאות מאושרות עם תאריך אישור, מצב כשירות וסימון הגרסה המוצגת.
- לבצע מעבר באמצעות ה־URL של המזהה הנבחר, כדי שקישור ישיר ורענון ישמרו את הבחירה.
- להציג סימון הגשה רק מתוך רשומות Submission, עם קישור לגרסה ולארטיפקט המדויקים. הגשה חיצונית אינה הוכחה לגרסה פנימית.
- לאפשר בחירת שתי גרסאות להשוואת טקסט, תוך הפרדה משינויי נוסח המשרה שב־UX-13.
- לשמר אזהרות הקשר היסטורי ולוודא שהורדה או הגשה מתייחסות לגרסה המוצגת ולכשירותה, בלי להחליף אותה אוטומטית בחדשה ביותר.

**תנאי סיום:** כל גרסה מאושרת זמינה ניתנת לפתיחה ולהשוואה. אפשר לזהות מה נמסר בכל הגשה מתועדת; מידע שלא נרשם נשאר חסר. המעבר אינו משנה גרסאות, ארטיפקטים או היסטוריית הגשות.

**אימות למסירה:** גרסה יחידה, כמה גרסאות, Ready היסטורי, גרסה ללא PDF כשיר, כמה הגשות והגשה חיצונית. להתחיל מ־`RevisionPage.test.tsx`; שינוי projection או חתימת API מחייב pipeline ללא AI, ושינוי נתיב artifact מחייב golden ודפדפן.

### UX-18 — כיווניות ויישור תוכן בטיוטה

- [ ] **אימות תרחישי BiDi והשלמת המשימה**

**מטרה:** למנוע שיבושי פיסוק ומספרים בטקסט קורות חיים באנגלית בתוך מעטפת ממשק בעברית, תוך תמיכה בטיוטות בעברית.

**מצב קיים:** ב־`DraftClaimRow` כבר יש `dir="auto"` על שדה העריכה והטקסט המוצג; הוא קיים גם בחלק מתוכן הסעיפים. היעדר כיווניות מוחלט אינו ממצא מאומת. צריך לבדוק יישור וטקסט שמתחיל במספר, סימן או שם עברי.

**אזורי עבודה:** `DraftClaimRow`, `DraftSectionCard`, `DraftOutlineEditor`, שדות הוספת שורות ורכיבי הצגת עובדות תומכות.

**צעדים לביצוע:**

- לבדוק את כיוון התוכן לפי שפת הטיוטה ואת המדיניות לטקסט מעורב; לא להחיל LTR על כל הטיוטות.
- באזורי תוכן אנגלי לקבוע `dir="ltr"` ויישור שמאלה כשזה מתאים לשפת המסמך; בתוכן עברי לשמר RTL ויישור מתאים.
- לבודד כתובות URL, אימייל ומזהים לטיניים כשנדרש. להשאיר תוויות, כפתורים ותגי מצב במעטפת RTL.
- לבדוק מקפים, סוגריים, אחוזים, טלפונים, תאריכים ופיסוק סופי במצב עריכה ובמצב קריאה.
- לוודא שכיווניות אינה משנה טקסט שמור, סדר שורות או תוכן שמועתק ללוח.

**תנאי סיום:** טיוטות באנגלית ובעברית מוצגות ונערכות בכיוון וביישור הצפויים; תווים מיוחדים נשארים במקומם והתוויות בעברית אינן מתהפכות.

**אימות למסירה:** להרחיב את `DraftClaimRow.test.tsx` רק לפערים חסרים ולמסור בדיקה חזותית בתרחישי טקסט מעורב. אם משתנה renderer או HTML/PDF מופק, למסור גם golden ודפדפן; תיקון כיווניות במעטפת העורך אינו אמור לשנות ארטיפקט היסטורי.

### UX-21 — הסרת תלות ב־sessionStorage להמשכי עבודה אוטומטיים

- [ ] **השלמת המשימה**

**מטרה:** המשך מניתוח ליצירת טיוטה וניווט לעורך יעבוד גם כש־Storage אינו זמין, בלי פעולות כפולות או ניווט למועמדות אחרת.

**מצב קיים:** `features/preparation/api/mutations.ts` קורא וכותב ישירות מפתחות `stage-e:auto-draft`, `stage-e:draft-navigation` ו־`stage-e:auto-draft-decision`. קיימים גם latch מקומי ו־idempotency לפי מקורות הניתוח והתוכנית. המפתחות כוללים מזהי Operation או מועמדות, ו־sessionStorage מבודד בדרך כלל בין טאבים; טענת דריסה גלובלית בין טאבים אינה מוכחת. הפער המאומת הוא תלות בגישה ישירה ל־Storage, ויש לבדוק גם רענון ופתיחת טאב מתוך טאב אחר.

**מפרט מחייב:** `docs/spec/state-and-use-cases.md`, הקשר פעיל, Operations והרשאות; חוזה האוטומציה ב־Settings. אין לשנות את מדיניות ההמשך האוטומטי במסגרת החלפת מנגנון אחסון.

**אזורי עבודה:** `useAutomaticDraft` ויתר hooks ב־`preparation/api/mutations.ts`, `useWatchedOperation`, React Query וניתוב שלבי ההכנה.

**צעדים לביצוע:**

- למפות בנפרד סימוני תזמון, כוונת המשך וניווט שכבר בוצע; להגדיר מה צריך לשרוד רענון ומה הוא מידע מקומי בלבד.
- לבסס מצב עבודה על ApplicationDetail ו־Operation מהשרת, עם cache ומנגנון מקומי שמבודדים לפי מועמדות ומקורות מפורשים.
- לשמר idempotency בשרת; מטמון בזיכרון לבדו אינו מונע כפילות בין טאבים או אחרי רענון.
- אם מידע חיוני לשחזור חסר בשרת, לתאר את הפער בחוזה לפני הסרת הסימונים. לא להסיק הסכמה להמשך רק מכך שאין טיוטה.
- למנוע ניווט מאוחר של Operation ישן לאחר מעבר למועמדות אחרת ולהגדיר מה קורה אחרי cancellation, failure או החלפת מקורות.
- להפריד מהמשימה אחסון שחזור עריכות ב־`useDraftAutosave` והעדפות לוח; הסרת מפתחות ההמשך אינה אישור למחיקת buffers מקומיים.

**תנאי סיום:** Storage חסום אינו מפיל את התהליך; רענון ושני טאבים אינם יוצרים טיוטה כפולה או ניווט שגוי. החלטות וחוסמי אימות נשארים מחייבים, ומדיניות האוטומציה נשמרת.

**אימות למסירה:** getItem/setItem שזורקים, שתי מועמדויות בשני טאבים, אותה מועמדות בשני טאבים, רענון בין analyze ל־draft, החלטה פתוחה ותוצאה מאוחרת. לתאם עם UX-06; הרחבת projection או חתימה מחייבת pipeline ללא AI.

### UX-22 — איחוד העדפת ערכת הנושא עם Settings

- [ ] **השלמת המשימה**

**מטרה:** לשמור Theme לצד צפיפות וגודל טקסט ולהציע שליטה אחידה במסך ההגדרות ובמעטפת.

**מצב קיים:** `ThemeToggle` שומר `cv-theme` ב־localStorage, עם fallback להעדפת מערכת וטיפול ב־Storage חסום. צפיפות וגודל טקסט נקראים מ־Settings בשרת. זהו פיצול אמיתי במקורות ההעדפה; המוצר מקומי וללא חשבון, ולכן אין להבטיח סנכרון בין התקנות או מכשירים שאינם פונים לאותו שרת מורשה.

**מפרט מחייב:** `docs/spec/product-spec.md`, סעיף 15 להעדפות UI וגבולות הריצה; `docs/spec/state-and-use-cases.md`, סעיף 19a; `docs/spec/architecture.md` להרחבת schema ו־Settings.

**אזורי עבודה:** `ThemeToggle`, `AppLayout`, `SettingsForm`, חוזי Settings, persistence, סוגי OpenAPI ואתחול Theme לפני הצגת הממשק.

**צעדים לביצוע:**

- להגדיר ערכים מפורשים כגון system / light / dark ולבחור ברירת מחדל שתואמת את ההתנהגות הנוכחית.
- להוסיף את ההעדפה לחוזה Settings ולהציג בורר במסך ההגדרות; שני הבוררים ישתמשו באותו מקור אמת ובטיפול בקונפליקט של UX-20.
- להגדיר העברה מהעדפה מקומית קיימת בלי לדרוס העדפה שכבר נשמרה בשרת ובלי ששני דפדפנים ייבאו ערכים סותרים אוטומטית.
- לשמר תצוגה מהירה בזמן טעינת Settings ולמנוע הבהוב Theme; מטמון מקומי, אם נשאר, אינו מקור אמת נוסף.
- להגדיר תצוגה מקדימה, כשל שמירה, החזרה לערך השמור ותגובה לשינוי העדפת מערכת במצב system.

**תנאי סיום:** מסך ההגדרות והבורר במעטפת משקפים אותו ערך שמור. פתיחת דפדפן נוסף מול אותה התקנה מורשית מקבלת את העדפת השרת. כשל שמירה אינו מוצג כהצלחה והעדפת המערכת פועלת כשנבחרה.

**אימות למסירה:** שלושת המצבים, העדפה מקומית ישנה, Storage חסום, שינוי מערכת, טעינה ראשונית וקונפליקט. הרחבת Settings מחייבת pipeline ללא AI; אם יש הגירת Alembic, למסור topology, שדרוג מסד ריק והבדל schema. Theme של המעטפת אינו אמור לשנות PDF קיים.

### UX-23 — חיווי ניתוק רשת וחוסר זמינות השרת

- [ ] **השלמת המשימה**

**מטרה:** להציג חיווי גלובלי ברור בכשל תקשורת ולשמור עריכות מקומיות עד שאפשר להמשיך.

**מצב קיים:** ב־`AppLayout` אין חיווי תקשורת גלובלי. יש לאמת את התנהגות React Query בעת ניתוק. `online/offline` מתארים את מצב הרשת המדווח בדפדפן ואינם מוכיחים שה־API זמין; בריצה מקומית אובדן אינטרנט גם אינו בהכרח מונע שימוש בשרת או בזרימה הדטרמיניסטית.

**אזורי עבודה:** `AppLayout`, שכבת API, מדיניות Query/Mutation, חיווי נגיש ו־autosave של טיוטות.

**צעדים לביצוע:**

- להוסיף חיווי רשת בדפדפן ולהפריד אותו מ„אין חיבור לשרת”, שנקבע לפי כשלי תקשורת ממשיים או בדיקת זמינות קיימת ומתונה.
- לא לסמן 4xx, שגיאת ולידציה או כישלון Operation כניתוק שרת; כשל ספק AI אינו בהכרח כשל של ה־API.
- להציג הודעה נגישה ודרך בדיקה מחדש, בלי להוסיף אותה התרעה בכל polling tick.
- כשאין חיבור ל־API, להשהות או לחסום פעולות שרת מתאימות עם הסבר, תוך שימור עריכה מקומית ותור autosave. לא לחסום פעולות מקומיות או את המנוע המקומי רק בגלל navigator.onLine.
- לאחר התאוששות לרענן projections ו־Operations ולהמשיך שמירה לפי ETag. לא לשדר מחדש פקודות לא־אידמפוטנטיות אוטומטית כשלא ידוע אם התקבלו.
- לשמר הודעות שגיאה ספציפיות שמסבירות כשל פעולה, גם כשיש חיווי גלובלי.

**תנאי סיום:** ניתוק רשת ושרת שאינו זמין מוצגים באופן מובחן. עריכות אינן אובדות, פעולות לא מוכפלות אחרי חיבור מחדש, וזרימה מקומית זמינה נשארת שימושית ללא אינטרנט כשאינה תלויה ב־AI.

**אימות למסירה:** רשת מנותקת עם API מקומי זמין, שרת כבוי כשהרשת online, בקשה עם תשובה לא ודאית, התאוששות, autosave ממתין ו־Operation שכבר רץ בשרת. להתחיל מ־`AppLayout.test.tsx` ומכיסוי מדיניות API/שמירה; שינוי זרימה או חוזה מחייב בירור מפרט, ושינוי חתימה או projection מחייב pipeline ללא AI.

## סדר ביצוע מומלץ

1. **UX-11 — פתרון החלטות מתוך הטיוטה:** לתת דרך מפורשת להתקדם כשאישור חסום.
2. **UX-05 — עדכון אחרי אישור עובדה:** להשלים משוב בתהליך פתרון שורה לא נתמכת.
3. **UX-06 + UX-16 — כניסה והמשך תהליך:** שחזור הקשר והתאוששות מכשל טעינה; להתחיל מאימות הממצא ב־UX-16.
4. **UX-03 — הגדרות התאמה:** אחרי בירור החוזה וההשפעה על ההקשר הפעיל.
5. **UX-01 + UX-04 — שימוש יומיומי:** קריאות המסמך והשלמת חוויית פעולות הרקע.
6. **UX-02 + UX-12 — שליטה בעריכה:** לתכנן יחד את ההשפעה על autosave, סדר והיסטוריית עריכות; לממש כל משימה בגבול ברור.
7. **UX-13 — היסטוריית משרות:** להשלים ליד UX-09 אם עובדים על עדכון נוסח משרה.
8. **UX-14 + UX-15 — ניהול עובדות:** אחרי בירור חוזי תיקון פרטי קשר ופעולות קבוצתיות.
9. **UX-20 — קונפליקט בהגדרות:** לספק התאוששות לפני הרחבת טופס Settings. UX-19 מתחיל בבדיקת הכיסוי הקיים; לקדם תיקון רק אם נמצא פער בייצוא האמיתי.
10. **UX-21 — המשכי עבודה:** לתאם עם UX-06 ולשמר מדיניות אוטומציה ו־idempotency.
11. **P2 לפי צורך:** UX-17 להיסטוריית גרסאות, UX-18 לכיווניות, UX-07 להעתקה ו־UX-08 לחיפוש; UX-22 אחרי UX-20. UX-23 יכול להקדים עבודה על autosave אם נמצא אובדן או שידור כפול. UX-10 יכול להקדים משימה שנוגעת בדיאלוג; UX-18 יכול להקדים עבודת עריכה אם שיבוש שוחזר.
